/** Round length in milliseconds (15s). */
export const GAME_DURATION_MS = 15_000;

/** When remaining time is at or below this, the “last 10 seconds” animation runs. */
export const PHASE_LAST_10_MS = 10_000;

/** When remaining time is at or below this, the stronger “last 5 seconds” animation runs. */
export const PHASE_LAST_5_MS = 5_000;

/** Imaginary BTC credited per tap (1 BTC per tap). */
export const BTC_PER_TAP = 1;
